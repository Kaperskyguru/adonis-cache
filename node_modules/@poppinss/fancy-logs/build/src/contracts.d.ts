import { Colors } from '@poppinss/colors/build/src/Base';
/**
 * Shape of the acceptable message node
 */
export declare type MessageNode = {
    prefix?: string;
    suffix?: string;
    icon?: boolean;
    color?: boolean;
    underline?: boolean;
    message: string | Error;
};
export declare type DeferredMessageNode = {
    action: keyof ActionsList;
    args: any[];
} & MessageNode;
/**
 * Loggable actions
 */
export declare type ActionNames = 'success' | 'fatal' | 'error' | 'warn' | 'info' | 'complete' | 'pending' | 'create' | 'update' | 'delete' | 'watch' | 'start' | 'stop' | 'compile' | 'skip';
/**
 * Action definition
 */
export declare type Action = {
    color: keyof Colors;
    badge: string;
    logLevel: 'info' | 'error';
};
/**
 * Shape of list of actions
 */
export declare type ActionsList = {
    [action in ActionNames]: Action;
};
