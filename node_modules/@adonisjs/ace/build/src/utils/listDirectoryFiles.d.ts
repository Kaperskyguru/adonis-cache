import { CommandsListFilterFn } from '../Contracts';
/**
 * Returns an array of Javascript files inside the current directory in
 * relative to the application root.
 */
export declare function listDirectoryFiles(scanDirectory: string, appRoot: string, filterFn?: CommandsListFilterFn): string[];
