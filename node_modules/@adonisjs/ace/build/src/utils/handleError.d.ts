import { Logger } from '@poppinss/fancy-logs';
/**
 * Handles the command errors and prints them to the console.
 */
export declare function handleError(error: any, callback?: ((error: any, loggerFn: Logger) => void | Promise<void>)): void;
