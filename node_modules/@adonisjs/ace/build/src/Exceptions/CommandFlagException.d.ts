import { Exception } from '@poppinss/utils';
import { CommandConstructorContract } from '../Contracts';
/**
 * Raised when an the type of a flag is not as one of the excepted type
 */
export declare class CommandFlagException extends Exception {
    command?: CommandConstructorContract;
    argumentName: string;
    exceptedType: string;
    /**
     * Flag type validation failed.
     */
    static invoke(prop: string, expected: string, command?: CommandConstructorContract): CommandFlagException;
}
