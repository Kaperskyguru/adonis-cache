import { Exception } from '@poppinss/utils';
import { CommandConstructorContract } from '../Contracts';
/**
 * Raised when an argument is missing but excepted
 */
export declare class CommandArgumentException extends Exception {
    command: CommandConstructorContract;
    argumentName: string;
    /**
     * A required argument is missing
     */
    static invoke(name: string, command: CommandConstructorContract): CommandArgumentException;
}
